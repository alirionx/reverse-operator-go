---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: reverseproxyentries.reverse-proxy.app-scape.de
spec:
  group: reverse-proxy.app-scape.de
  names:
    kind: ReverseProxyEntry
    listKind: ReverseProxyEntryList
    plural: reverseproxyentries
    singular: reverseproxyentry
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ReverseProxyEntry is the Schema for the reverseproxyentries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ReverseProxyEntry
            properties:
              ingress:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  className:
                    default: nginx
                    type: string
                  host:
                    type: string
                  secretName:
                    type: string
                  targetProtocol:
                    default: http
                    type: string
                  tls:
                    default: false
                    type: boolean
                required:
                - className
                - host
                - targetProtocol
                - tls
                type: object
              target:
                properties:
                  endpoints:
                    items:
                      type: string
                    minItems: 1
                    type: array
                  port:
                    type: integer
                required:
                - endpoints
                - port
                type: object
            required:
            - ingress
            - target
            type: object
          status:
            description: status defines the observed state of ReverseProxyEntry
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
